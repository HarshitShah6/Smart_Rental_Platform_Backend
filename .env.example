# PostgreSQL connection string (use Supabase or local)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smart_rental

# Firebase Admin SDK JSON (service account key as single-line JSON string)
# Get this from Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# FIREBASE_ADMIN_SDK={"type":"service_account","project_id":"..."}

# JWT secret for internal token signing (use a strong random string in production)
JWT_SECRET=supersecretkey123

# ML microservice base URL (FastAPI)
ML_BASE=http://localhost:8000

# Server port
PORT=4000
